#include <stdio.h>
/**
 * main - is an entry point
 *
 * Description: prints all the alphabet letters
 *
 * Return: Is always 0 (success)
*/
int main(void)
{
	char alphabet == 'a';
	while (alphabet <= 'z')
	{
		putchar(alphabet);
		++alphabet;
	}
	putchar('\n');
	return (0);
}

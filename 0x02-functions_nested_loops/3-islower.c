#include "main.h"
/**
 * _islower - return 1 if c is lowercase,
 * else return 0
 *
 *@c - chara in ASCII code
 *
 * Return - 1 if its lowercase,
 * 0 if not
*/
int _islower(int c)
{
	return (c >= 97 && c <= 122);
}

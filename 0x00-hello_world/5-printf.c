#include <stdio.h>
/**
 * main - is an entry point
 *
 * Description: a C program that prints using the printf fuction
 *
 * Return: is always 0 (Success)
*/
int main(void)
{
	printf("with proper grammar, but the outcome is a piece of art,\n");
	return (0);
}
